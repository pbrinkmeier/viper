package edu.kit.ipd.pp.viper.controller;

/**
 * Enumeration of all supported image export formats.
 * */
public enum ImageFormat {
    /** SVG Export-Type (Vector graphic) */
    SVG,
    /** PNG Export-Type (Pixel graphic) */
    PNG
}