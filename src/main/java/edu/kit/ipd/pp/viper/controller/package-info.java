/**
 * The controller package acts as an intermediary between the model and view classes.
 * Specifically, it contains a {@link Command} for every user interaction and data
 * management facilities such as the {@link InterpreterManager} and the
 * {@link LanguageManager}.
 */
package edu.kit.ipd.pp.viper.controller;
